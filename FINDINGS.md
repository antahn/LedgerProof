# FINDINGS

**This file records only measured results.**

Every bug listed here was found by the fault-injection harness (or a test), and
every entry carries three things: a minimal reproduction, a root-cause diagnosis
in prose, and the fix. Counts are truthful — if the harness found zero breaks in
a category, that is what this file says. Anything that was skipped, sampled, or
capped is recorded in the Coverage notes section, because silent truncation
reads as "covered everything."

## Findings

*(none yet — the harness has not run)*

## Coverage notes

*(none yet)*
